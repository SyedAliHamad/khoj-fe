# API base URL - required when frontend and backend run on different servers (e.g. Vercel + separate backend)
# Leave unset for local dev (uses /api, proxied to localhost via next.config rewrites)
NEXT_PUBLIC_API_BASE_URL=https://api.khoj.com.pk

# Optional: shipping config (defaults shown)
#NEXT_PUBLIC_SHIPPING_FREE_THRESHOLD=5000
NEXT_PUBLIC_SHIPPING_FEE=250
